# 🤿 Дневник погружений (Logbook)

Приложение для ведения дневника подводных погружений: добавление, просмотр, редактирование и анализ записей. Данные хранятся локально в браузере.

## 🎯 Возможности

- ✍️ **Добавление погружений** — дата, место, глубина, длительность, оценка, заметки, необязательное время начала
- ✏️ **Редактирование** — прямо в модальном окне, без его закрытия
- 📋 **Два режима** — «Редактирование» (форма + карточки) и «Просмотр» (таблица)
- 🔎 **Модальное окно** — детали погружения с анимацией открытия и закрытия
- 📊 **Статистика** — всего погружений, максимальная глубина, суммарное время под водой
- 🔍 **Поиск и сортировка** — в табличном режиме по месту, с пагинацией
- ⭐ **Рейтинг** — 5-звёздочная шкала
- 💾 **localStorage** — данные сохраняются между сессиями

## ✨ Особенности реализации

- **Модульная архитектура** — компоненты сгруппированы по функциональности
- **Переиспользование** — `ActionButton`, `Stats`, `FormInput`, `Stars`
- **Валидация формы** — обязательны все поля, кроме времени начала и заметок; нельзя выбирать будущие даты
- **Анимации модалки** — плавное появление и исчезновение с учётом `prefers-reduced-motion`

## 🛠 Технологии

- **React 19.2** — UI
- **TypeScript 6.0** — типизация
- **Vite 8.1** — сборка и dev-сервер
- **Oxlint** — линтер

## 🚀 Запуск

```bash
# Установка зависимостей
npm install

# Dev-сервер (http://localhost:5173)
npm run dev

# Сборка
npm run build

# Предпросмотр сборки
npm run preview

# Линтер
npm run lint
```

## 📁 Структура проекта

```
src/
├── App.tsx                 # Корневой компонент, переключение режимов
├── main.tsx                # Точка входа
├── App.css                 # Стили приложения
├── index.css               # Глобальные стили
├── components/
│   ├── DiveForm/           # Форма добавления и редактирования
│   │   ├── index.tsx
│   │   ├── types.ts
│   │   ├── const.ts
│   │   └── validation.ts
│   ├── DiveList/           # Список карточек и модальное окно
│   │   ├── index.tsx
│   │   ├── types.ts
│   │   ├── DiveCard.tsx
│   │   └── DiveModal.tsx
│   ├── DiveTable/          # Таблица для режима просмотра
│   │   ├── index.tsx
│   │   └── types.ts
│   └── Shared/
│       ├── ActionButton.tsx
│       ├── Stats.tsx
│       ├── Stars.tsx
│       └── FormInput/
│           ├── index.tsx
│           └── types.ts
├── hooks/
│   └── useDiveLog.ts       # CRUD и статистика, localStorage
├── types/
│   └── dive.ts             # Интерфейсы Dive и DiveInput
└── utils/
    └── format.ts           # Форматирование дат и времени
```

## 📝 Формат данных

Каждое погружение (`Dive`):

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `string` | Уникальный идентификатор |
| `date` | `string` | Дата (ISO, `YYYY-MM-DD`) |
| `time` | `string` | Время начала (`HH:MM`), необязательно |
| `site` | `string` | Место / сайт |
| `maxDepthM` | `number` | Максимальная глубина, м |
| `durationMin` | `number` | Длительность, мин |
| `notes` | `string` | Заметки, необязательно |
| `rating` | `number` | Оценка от 1 до 5 |

Данные сохраняются в `localStorage` под ключом `dive-logbook`.

## 📄 Лицензия

MIT
